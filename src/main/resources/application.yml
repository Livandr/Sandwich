spring:
  datasource:
    url: jdbc:postgresql://localhost:55001/sandwich_db
    username: postgres
    password: postgrespw
    driver-class-name:  org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

